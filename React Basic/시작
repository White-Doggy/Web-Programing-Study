도입 배경
DOM에 대해 직접 설정하는 이벤트가 유지보수에 따라 복잡해지는 문제

특정 상황에 대해 규칙을 정하는 것이 아니라, view 자체를 생성
+ virtual DOM을 통해 성능 저하 없이 이를 구현

ex) 특정 상황에 대한 virtual DOM과 현재 상태를 비교하고
여기서 차이가 있는 부분에 대해서만 생성을 통해 화면을 구현

-> UI 업데이트가 관건이 아니라 어떻게 보여주는가가 관건


컴포넌트 ( 일종의 UI 조각 )


